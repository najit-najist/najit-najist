import { describe, expect, it } from 'vitest';
import { MailService } from '../../src/services/Mail.service';

describe('plugins', () => {
  describe('mail', () => {
    it.todo('Imlement that it actually sends correct info to mailer');
    // TODO: deprecated
    // it('should render relative', async () => {
    //   const name = 'John';
    //   const templateFileContent = `Hello ${name}`;
    //   const res = await MailService..render('test', {
    //     name,
    //   });
    //   expect(res).toBe(templateFileContent);
    // });
  });
});
